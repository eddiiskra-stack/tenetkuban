
name: Build & Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy to Yandex Cloud
        env:
          YANDEX_TOKEN: ${{ secrets.YANDEX_TOKEN }}
        run: |
          # This is a placeholder for the Yandex Cloud CLI deployment command.
          # You may need to install the 'cdi' CLI first.
          echo "Deploying to Yandex Cloud..."
          # cdi deploy --source .next/ --target my-yandex-bucket
          echo "Deployment successful (simulation)."

